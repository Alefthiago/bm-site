// Util //
import Globals from "@/components/globals";
// Util. //

const pageSoluctions = () => {
    return (
        <div className={`${Globals.default_style_page}`}>
            <h1>Page Soluctions</h1>
        </div>
    );
}

export default pageSoluctions;
