// Util //
import Globals from "@/components/globals";
// Util. //

const pageContact = () => {
    return (
        <section className={`${Globals.default_style_page}`}>
            <h1>Contato</h1>
        </section>
    );
}